registrationModule.controller('pagoController', function($scope, $rootScope, $location,filtroRepository, pagoRepository, alertFactory, localStorageService) {
   

    $scope.init = function() {
        
    }

});